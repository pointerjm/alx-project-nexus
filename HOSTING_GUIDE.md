# 🚀 Hosting Guide — Django on Vercel (with PostgreSQL)

This guide explains how to deploy the ecommerce backend to **Vercel** using serverless functions.

---

# 1. Prerequisites
- Vercel account
- GitHub repository
- Cloud PostgreSQL (Railway, Supabase, Render, Neon, etc.)
- `vercel` CLI installed:
```bash
npm i -g vercel
